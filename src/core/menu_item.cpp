#include "core/menu_item.h"

// MenuItem is a plain struct — nothing to implement beyond the header.
// This TU exists so the static library always has at least one symbol
// from every header, which simplifies downstream linking.

namespace viewtouch {
}  // namespace viewtouch
